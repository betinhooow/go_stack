{% extends "_layouts/default.njk" %}

{% block body %}
  <div class="content">
    <strong> Hello, {{ user.name }}</strong>
    <p>Mark a session choosing a following professional</p>

    <ul class="providers">
      {% for provider in providers %}
        <li class="provider">
          <div>
            <img src="" />
            <strong>{{ provider.name }}</strong>
          </div>

          <a href="">+</a>
        </li>
      {% endfor %}
    </ul>

    <a href="/app/logout" class="logout"> Logout </a>
  </div>
{% endblock %}
